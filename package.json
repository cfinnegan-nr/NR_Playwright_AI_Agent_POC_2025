{
  "name": "netreveal-playwright-tests",
  "version": "1.0.0",
  "description": "Playwright TypeScript tests for NetReveal application",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:e2e": "npx playwright test tests/ui",
    "test:api": "npx playwright test tests/api",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "lint": "eslint . --ext .ts"
  },
  "keywords": [
    "playwright",
    "testing",
    "automation",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.42.1",
    "typescript": "^5.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.25",
    "eslint": "^8.57.0",
    "eslint-plugin-playwright": "^1.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}